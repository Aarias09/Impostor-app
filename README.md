    # El Impostor

    Proyecto React + Tailwind + Framer Motion + XLSX.js

    ## Instrucciones rápidas

    1. Instalar dependencias:

```
npm install
```

2. Ejecutar en desarrollo:

```
npm run dev
```

Abre la URL que muestre Vite (por defecto http://localhost:5173)

## Qué incluye este ZIP
- Proyecto frontend listo (estructura en `src/`)
- .gitignore
- README con instrucciones

## Notas
Puedes inicializar un repo con `git init` y hacer commit. Si querés que lo inicialice por vos, decímelo.
